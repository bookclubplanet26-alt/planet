# 🪐 북클럽 플래닛 (Planet) UI/UX 추후 개선 계획서

본 문서는 전형적인 AI 양산형 웹 디자인(인위적인 그라디언트, 과도한 여백, 낮은 텍스트 대비 등)을 탈피하고, **실제 상용 서비스(카카오톡 모임, 당근 모임 등) 수준의 모바일 가독성과 사용성을 확보하기 위한 UI/UX 개선 항목**을 정리한 문서입니다.

---

## 📌 1. 개선 배경 및 목표

* **배경**:
  * 모바일 환경에서 모임 멤버들이 야외나 이동 중에 접속했을 때 핵심 정보(일시, 장소, 잔여석)가 한눈에 들어오지 않는 문제.
  * 불필요한 그라디언트, 코드 블록(백틱) 남용, 지나치게 큰 알림 박스로 인한 스크롤 피로도 증가.
* **목표**:
  * **모바일 최적화 가독성**: 스크롤 한 번에 모임의 상태와 정보를 스캔할 수 있는 시각적 위계 확립.
  * **현실적이고 단정한 디자인**: AI 템플릿 느낌을 벗어나 신뢰감을 주는 모던 플랫(Flat) & 고대비 스타일 적용.

---

## 📋 2. 상세 개선 항목

### ① 모임 카드 UI 리팩토링 (`views/schedule.py`) - ✅ [1단계 완료]
* [x] **핵심 정보 가독성 개선 (백틱 블록 제거)**
  * 기존: 일시 `` `2026-09-12` `14:00` `` (작고 흐린 회색 인라인 코드 형태)
  * 변경: **15~16px 굵은 텍스트 + 고대비 폰트(#222222)** 로 날짜, 시간, 장소를 명확하게 분리 표시
* [x] **컴팩트한 상태 배지(Chip) 적용**
  * 기존: 화면을 크게 차지하는 `st.success`("🟢 신청가능 (3/6명)"), `st.warning` 박스
  * 변경: 카드 우측 상단 또는 제목 옆에 **작고 세련된 태그 칩**(`[🟢 신청가능 3/6]`, `[⏳ 대기신청]`, `[🔴 마감]`) 형태로 배치하여 세로 스크롤 길이 단축
* [x] **독립된 카드 경계(Card Surface) 부여**
  * 모임마다 뚜렷한 1px 테두리(Border)와 부드러운 패딩을 부여하여 모임 간 구분을 명확히 함
* [x] **오픈카톡/신청 버튼의 직관적인 액션 영역**
  * 오픈카톡방 주소, 참가 신청, 취소 버튼을 모바일 엄지 영역(Thumb Zone)에 맞춰 하단에 단정하게 정돈

---

### ② 글로벌 스타일 및 타이포그래피 정돈 (`styles.py`) - ✅ [2단계 완료]
* [x] **인위적인 그라디언트 제거 및 모던 플랫화**
  * 홈 화면의 헤더 박스(`linear-gradient(#4A3525, #6D4C41)`)를 미니멀하고 단정한 서페이스로 변경
  * 지나치게 화려한 색상 대신 차분한 브라운/아이보리/다크그레이 톤의 절제된 컬러 팔레트 구축
* [x] **명도 대비(Contrast) 최적화 (WCAG 4.5:1 이상)**
  * 모바일 화면 밝기가 낮거나 야외 햇빛 아래에서도 본문이 또렷하게 보이도록 서브 텍스트 명도 상향
* [x] **다크/라이트 모드 대응 완성도 제고**
  * 사용자의 모바일 OS 설정(다크모드)에 따라 텍스트나 카드가 흐려지지 않도록 변수/색상 값 고정 처리 점검

---

### ③ 서브 페이지 및 마이크로 인터랙션 정돈 - ✅ [3단계 완료]
* [x] **모임 소개 페이지 (`views/intro.py`)**
  * 하단 인스타그램 바로가기 버튼의 화려한 그라디언트를 앱의 전체 톤앤매너와 조화로운 플랫 디자인으로 정돈
* [x] **메인 메뉴 (`바로가기.py`)**
  * 홈 화면 메뉴 버튼들의 아이콘과 텍스트 균형을 가다듬어 터치 반응성 향상
* [x] **로딩 및 처리 상태 피드백 간소화**
  * 신청/취소 시 화면 전체가 깜빡이거나 긴 대기 문구 대신, 간결한 토스트(`st.toast`) 및 인라인 스피너로 자연스러운 전환 제공

---

### ④ 성능 최적화 및 보안 보강 - ✅ [4단계 완료]
* [x] **`gspread` 클라이언트 인스턴스 캐싱 (`services/sheets.py`)**
  * `@st.cache_resource`를 적용하여 매 시트 API 요청마다 반복되던 서비스 계정 키 파싱 및 OAuth 토큰 인증 지연(수백 ms) 제거
* [x] **불필요한 워크시트 메타데이터 전수 조회 제거 (`services/sheets.py`)**
  * `회원목록`, `출석목록`, `모임목록` 접근 시 전체 탭을 탐색하던 `sh.worksheets()` 호출을 제거하고 `try/except` 직접 접근으로 API 왕복 1회(0.5~1.5초) 단축
* [x] **`importlib.reload` 오버헤드 제거 (`바로가기.py`)**
  * 모든 클릭 및 rerun마다 모듈 전체를 재컴파일·재로드하던 로직을 제거하여 메인 메뉴 및 화면 전환 반응성 극대화
* [x] **회원 자격 및 예치금 중복 연산 제거 (`views/schedule.py`, `services/accounting.py`)**
  * 페이지 렌더링당 3~4회씩 반복되던 회원 데이터프레임 순회 및 출석 횟수 카운트를 1회 사전 계산 후 파라미터로 공유하도록 최적화
* [x] **모임 안내문구 XSS 방어 및 오픈카톡 프로토콜 검증 (`views/schedule.py`)**
  * 모임 설명, 참가자 닉네임, 도서명 등에 `html.escape`를 적용하고 오픈카톡 링크의 `https://` 유효성 검증 및 `rel="noopener noreferrer"` 추가
* [x] **결측치("nan", "none") 정제 유틸리티 보강 (`services/sheets.py`)**
  * `_clean_str()`를 통해 판다스 결측치가 문자열 `"nan"`으로 변환되어 화면에 노출되던 잠재적 버그 원천 차단

---

### ⑤ 모임 개설 안정성 및 데이터 정합성 강화 (`views/admin_meeting_create.py`) - ⏳ [진행 예정]
* [ ] **정규 모임 요일-날짜 일치 자동 검증**
  * '토요일 강남 (어텀)'을 선택하고 일요일/평일 날짜를 고르거나, '일요일 종각 (윈터블)' 선택 후 토요일 날짜를 고르는 운영 실수 방지
  * `m_date.weekday()`를 검증하여 요일 불일치 시 실시간 경고 안내 및 저장 차단
* [ ] **동일 날짜/모임 중복 개설 방지**
  * 같은 날짜에 동일한 모임이 이미 개설되어 있는지 대조하여 실수로 인한 중복 생성 방어
* [ ] **오픈 카카오톡방 링크 `https://` 자동 정제**
  * 모임장이 프로토콜 없이 `open.kakao.com/...`만 입력해도 자동으로 `https://` 접두어를 붙여 버튼 누락 방지
* [ ] **함수 호출 키워드 인자(`kwargs`) 통일**
  * `append_meeting_to_google_sheet_async` 호출 시 위치 인자 대신 키워드 인자를 사용하여 컬럼 순서 변경 오류 방지

---

### ⑥ 보안 및 동시성 장기 고도화 과제 - 📋 [백로그]
* [ ] **Google 계정 실제 OAuth/SSO 연동 검토**
  * 단순 이메일 텍스트 입력 대조 방식에서 Streamlit Cloud 내장 `st.user` (Google SSO) 또는 관리자 기능용 2차 PIN 번호 도입 검토
* [ ] **Apps Script `LockService` 기반 정원 초과 방지**
  * 잔여 1석에 동시 신청 발생 시 구글 시트 백엔드 단에서 락을 걸어 정원 초과 행을 자동으로 '대기'로 강제 전환
* [ ] **예외 로깅(`logging`) 체계 보강**
  * 시트 연동 실패 시 `except Exception: pass` 대신 에러 로그를 남겨 운영 콘솔에서 신속한 장애 추적 지원

---

## 🚀 3. 우선순위 및 실행 로드맵

1. **완료**: UI/UX 가독성 개편 (1~3단계) 및 API 응답속도/보안 최적화 (4단계)
2. **차기 우선 과제**: `views/admin_meeting_create.py` 모임 개설 요일 검증 & 중복 방지 & 카톡 링크 자동 보정 (5단계)
3. **중장기 과제**: 관리자 인증 고도화 및 백엔드 Apps Script 동시성 락 강화 (6단계)


# 🪐 북클럽 플래닛 (Planet) 주요 트러블슈팅 및 배포 주의사항 노트

본 문서는 프로젝트 개발 및 최적화 과정에서 겪었던 **치명적인 실수와 원인, 그리고 재발 방지를 위한 핵심 해결책**을 정리한 문서입니다.

---

## 🚨 1. [가장 치명적] Streamlit Cloud 변경사항 미반영 (모듈 캐싱 버그)

### 📌 발생 현상
* 로컬에서 코드를 수정하고 깃허브 원격 저장소(`origin/main`)에 정상 푸시했음에도, **실제 Streamlit Cloud 웹사이트에서는 새로 추가한 기능/UI가 전혀 나타나지 않고 구버전이 계속 노출됨**.

### 🔍 근본 원인
1. **진입점 감시 한계**: Streamlit Cloud는 메인 진입점 파일(`app.py`)이 수정되지 않고 하위 파일(`views/*.py`, `styles.py`)만 수정되면, 파일 변경을 감지하지 못하고 컨테이너 재시작을 생략함.
2. **파이썬 메모리 캐싱(`sys.modules`)**: Streamlit이 다시 실행되더라도 파이썬 인터프리터가 종료되지 않은 상태에서는 이미 메모리에 로드된 구버전 모듈(`views.schedule` 등)을 그대로 재사용함.
3. **성능 최적화 시 무심코 제거한 실수**: 이전 트러블슈팅에서 추가해두었던 `importlib.reload()`를 "임포트 오버헤드를 줄이겠다"는 잘못된 추측으로 제거하면서 재발함.

### ✅ 재발 방지책 (해결 완료)
* **`app.py` 진입점 버전 트리거 유지**: 코드 배포 시 필요시 `app.py` 주석의 빌드 버전을 갱신하여 Streamlit Cloud가 즉시 변경을 감지하도록 함.
* **`바로가기.py` 상단 강제 리로드 유지**:
  ```python
  import importlib
  import styles, views.calendar_widget, views.schedule
  importlib.reload(styles)
  importlib.reload(views.calendar_widget)
  importlib.reload(views.schedule)
  ```
  *(※ 실제 리로드 소요시간은 **1.3ms**에 불과하므로 성능에 영향을 주지 않으면서 배포 반영을 100% 보장함)*

---

## ⚠️ 2. 계좌번호 복사 기능의 과도한 엔지니어링 (UI 가독성 저해)

### 📌 발생 현상
* 모임 상세 카드에 계좌번호를 안내하면서, 눈에 너무 띄는 대형 코드 블록(`st.code`)이나 화면을 절반 이상 차지하는 하이라이트 박스를 삽입하여 **스크롤이 지나치게 길어지고 모바일 카드 가독성이 급격히 떨어짐**.

### 🔍 원인 및 실수
* 사용자의 단순한 니즈("계좌번호를 편하게 복사하고 싶다")를 넘어, 화려하고 거대한 UI 컴포넌트를 미리 얹으려는 **추측성 과잉 개발(Over-engineering)** 발생.
* 정규 모임(회비 없음)과 지정책 모임(예치금/비용 있음)의 성격 차이를 간과하고 일괄 노출하려 함.

### ✅ 해결책
* 지정책 모임에만 제한적으로 노출하도록 조건을 좁힘.
* 거대한 박스를 걷어내고, 인라인 텍스트 옆에 **작고 깔끔한 1클릭 복사 버튼(`📋`)** 하나만 심플하게 배치.

---

## ⚡ 3. 모임 카드 렌더링 시 중복 연산 (응답 속도 저하)

### 📌 발생 현상
* 모임 목록 화면이 뜰 때마다 1~2초 이상의 불필요한 렌더링 지연 및 버벅임 발생.

### 🔍 원인 및 실수
* `views/schedule.py`에서 모임 카드 10개를 그리는 `for` 루프 내부마다 `check_member_season_eligibility`(시즌 자격)와 `get_member_deposit_info`(예치금 계산)를 **매 카드마다 반복 호출**함.
* 동일한 로그인 유저에 대해 10번이나 중복 계산이 실행됨.

### ✅ 해결책
* 루프 외부에서 **단 1회만 계산(Pre-calculation)** 하여 변수에 담아두고, 각 카드에는 계산된 결과값만 넘겨주도록 구조 개선 (지연 0ms로 단축).

---

## 🔒 4. 구글 시트 저장 데이터의 정합성 검증 누락

### 📌 발생 현상
* 운영진이 모임을 개설할 때 '토요일 강남'을 선택하고 실수로 일요일 날짜를 고르거나, 카카오톡 오픈채팅 주소에 `https://` 프로토콜을 빠뜨려 버튼 링크가 동작하지 않는 문제.

### 🔍 원인 및 실수
* 프론트엔드 입력 폼에서 "사용자가 항상 정확하게 입력할 것"이라고 가정한 채 구글 시트로 바로 `append` 처리함.

### ✅ 해결책
* 개설 시 `date.weekday()`를 검사하여 **요일과 모임명이 일치하는지 실시간 검증** 및 에러 피드백.
* 오픈채팅 주소에 `https://`가 없으면 자동으로 접두어를 보정하여 링크 깨짐 원천 차단.
* 동일 날짜+동일 모임 중복 등록 방지 로직 구축.

---

## 🛡️ 5. 인라인 HTML/CSS 삽입 시 특수문자 및 보안 누락

### 📌 발생 현상
* 모임 설명, 책 제목, 신청자 이름 등에 `<`, `>`, `&`, 따옴표 등이 포함되었을 때 마크업 구조가 깨지거나 Streamlit HTML 렌더링 에러 발생.

### 🔍 원인 및 실수
* `st.markdown(..., unsafe_allow_html=True)` 사용 시 외부/시트 입력값을 가공 없이 문자열 포맷팅(`f"..."`)으로 직접 결합함.

### ✅ 해결책
* 사용자가 입력하거나 구글 시트에서 가져오는 모든 텍스트 필드에 `html.escape(text, quote=True)`를 필수로 적용.
* `float('nan')`이나 결측치는 공백 문자열(`""`)로 사전 정제.
